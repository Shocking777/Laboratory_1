[project]
name = "labs"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "typer",
]
[project.optional-dependencies]
dev = [
    "pytest>=8.4.2",
    "mypy",
    "ruff",
    "pre-commit",
]
